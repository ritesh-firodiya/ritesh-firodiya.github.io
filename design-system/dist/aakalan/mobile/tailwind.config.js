// GENERATED by scripts/build-tokens.mjs — DO NOT EDIT.
//
// design-system version: 1.0.0
// source: design-system/core.json + palettes/aakalan.json
//
// Edit the source and regenerate. `node scripts/build-tokens.mjs --check`
// regenerates into a temp dir and diffs, so a hand edit here fails the build
// rather than surviving as a twelfth source of truth.

/* ==== CANON: generated, byte-identical across this surface ==== */
const SCALES = {
  fontSize: {
    "2xs": ["var(--fs-2xs)", { lineHeight: "var(--lh-2xs)" }],
    xs: ["var(--fs-xs)", { lineHeight: "var(--lh-xs)" }],
    sm: ["var(--fs-sm)", { lineHeight: "var(--lh-sm)" }],
    base: ["var(--fs-base)", { lineHeight: "var(--lh-base)" }],
    body: ["var(--fs-body)", { lineHeight: "var(--lh-body)" }],
    lead: ["var(--fs-lead)", { lineHeight: "var(--lh-lead)" }],
    h3: [
      "var(--fs-h3)",
      { lineHeight: "var(--lh-h3)", letterSpacing: "var(--ls-h3)" },
    ],
    h2: [
      "var(--fs-h2)",
      { lineHeight: "var(--lh-h2)", letterSpacing: "var(--ls-h2)" },
    ],
    h1: [
      "var(--fs-h1)",
      { lineHeight: "var(--lh-h1)", letterSpacing: "var(--ls-h1)" },
    ],
    display: [
      "var(--fs-display)",
      {
        lineHeight: "var(--lh-display)",
        letterSpacing: "var(--ls-display)",
      },
    ],
    hero: [
      "var(--fs-hero)",
      { lineHeight: "var(--lh-hero)", letterSpacing: "var(--ls-hero)" },
    ],
  },
  borderRadius: {
    sm: "var(--radius-sm)",
    DEFAULT: "var(--radius)",
    lg: "var(--radius-lg)",
    xl: "var(--radius-xl)",
    "2xl": "var(--radius-2xl)",
    "3xl": "var(--radius-3xl)",
    card: "var(--radius-card)",
    pill: "var(--radius-pill)",
    phone: "var(--radius-phone)",
  },
  boxShadow: {
    sm: "var(--shadow-sm)",
    DEFAULT: "var(--shadow)",
    lg: "var(--shadow-lg)",
    phone: "var(--shadow-phone)",
  },
  letterSpacing: { label: "var(--tracking-label)", role: "var(--tracking-role)" },
  fontFamily: { sans: ["var(--font-sans)"], mono: ["var(--font-mono)"] },
};
/* ==== END CANON ==== */

/* Canonical colours: the same key set in every palette on this surface.
   A key outside design-system/core.json > shared.colorKeys fails lint check 3. */
const COLORS = {
  accent: {
    "50": "var(--accent-50)",
    "100": "var(--accent-100)",
    "200": "var(--accent-200)",
    "400": "var(--accent-400)",
    "500": "var(--accent-500)",
    "600": "var(--accent-600)",
    "700": "var(--accent-700)",
  },
  brand: {
    "50": "var(--brand-50)",
    "100": "var(--brand-100)",
    "200": "var(--brand-200)",
    "400": "var(--brand-400)",
    "500": "var(--brand-500)",
    "700": "var(--brand-700)",
    "900": "var(--brand-900)",
  },
  danger: {
    DEFAULT: "var(--danger)",
    bg: "var(--danger-bg)",
    fg: "var(--danger-fg)",
  },
  frame: "var(--frame)",
  info: { DEFAULT: "var(--info)", bg: "var(--info-bg)", fg: "var(--info-fg)" },
  ink: {
    "2": "var(--ink-2)",
    "3": "var(--ink-3)",
    "4": "var(--ink-4)",
    DEFAULT: "var(--ink)",
    inverse: "var(--ink-inverse)",
  },
  line: { DEFAULT: "var(--line)", strong: "var(--line-strong)" },
  muted: "var(--muted)",
  page: "var(--page)",
  success: {
    DEFAULT: "var(--success)",
    bg: "var(--success-bg)",
    fg: "var(--success-fg)",
  },
  sunken: "var(--sunken)",
  surface: "var(--surface)",
  warn: { DEFAULT: "var(--warn)", bg: "var(--warn-bg)", fg: "var(--warn-fg)" },
};

/* Reserved: colours that carry meaning in Aakalan and may not be
   reused for anything else. Rule 01 — the one six of nine sets arrived at
   independently. See design-system/palettes/aakalan.json for what each
   one means and why it is spoken for. */
const RESERVED = {
  cat: {
    food: "var(--cat-food)",
    "food-bg": "var(--cat-food-bg)",
    fun: "var(--cat-fun)",
    "fun-bg": "var(--cat-fun-bg)",
    home: "var(--cat-home)",
    "home-bg": "var(--cat-home-bg)",
    shop: "var(--cat-shop)",
    "shop-bg": "var(--cat-shop-bg)",
    stay: "var(--cat-stay)",
    "stay-bg": "var(--cat-stay-bg)",
    travel: "var(--cat-travel)",
    "travel-bg": "var(--cat-travel-bg)",
    util: "var(--cat-util)",
    "util-bg": "var(--cat-util-bg)",
  },
  neg: {
    DEFAULT: "var(--neg)",
    bg: "var(--neg-bg)",
    fg: "var(--neg-fg)",
    glow: "var(--neg-glow)",
  },
  pos: {
    DEFAULT: "var(--pos)",
    bg: "var(--pos-bg)",
    fg: "var(--pos-fg)",
    glow: "var(--pos-glow)",
  },
  settled: "var(--settled)",
};

/* Affordances specific to Aakalan: a card lift, a pressed edge, a
   floating action button. Shared geometry stays in SCALES. */
const SHADOWS = {  };

/* Gradients are background images, so bg-<name> resolves as an image utility. */
const GRADIENTS = { mark: "var(--mark)", "group-mark": "var(--group-mark)" };

/* Deep merge, one level. A shallow spread let RESERVED's brand group replace
   COLORS' brand group wholesale, so declaring a reserved brand-x-from
   silently deleted brand-500 and thirteen uses of bg-brand-500 painted
   nothing. Groups are merged key by key. */
const merge = (base, extra) => {
  const out = { ...base };
  for (const [group, value] of Object.entries(extra)) {
    out[group] =
      value && typeof value === "object" && out[group] && typeof out[group] === "object"
        ? { ...out[group], ...value }
        : value;
  }
  return out;
};

tailwind.config = {
  theme: {
    extend: {
      ...SCALES,
      boxShadow: { ...SCALES.boxShadow, ...SHADOWS },
      backgroundImage: GRADIENTS,
      colors: merge(COLORS, RESERVED),
    },
  },
};
