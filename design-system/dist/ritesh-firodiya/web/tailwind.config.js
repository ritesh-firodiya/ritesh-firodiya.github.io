// GENERATED by scripts/build-tokens.mjs — DO NOT EDIT.
//
// design-system version: 1.0.0
// source: design-system/core.json + palettes/ritesh-firodiya.json
//
// Edit the source and regenerate. `node scripts/build-tokens.mjs --check`
// regenerates into a temp dir and diffs, so a hand edit here fails the build
// rather than surviving as a twelfth source of truth.

/* ==== CANON: generated, byte-identical across this surface ==== */
const SCALES = {
  fontSize: {
    d1: [
      "var(--fs-d1)",
      { lineHeight: "var(--lh-d1)", letterSpacing: "var(--ls-d1)" },
    ],
    d2: [
      "var(--fs-d2)",
      { lineHeight: "var(--lh-d2)", letterSpacing: "var(--ls-d2)" },
    ],
    h2: [
      "var(--fs-h2)",
      { lineHeight: "var(--lh-h2)", letterSpacing: "var(--ls-h2)" },
    ],
    h3: [
      "var(--fs-h3)",
      { lineHeight: "var(--lh-h3)", letterSpacing: "var(--ls-h3)" },
    ],
    lead: ["var(--fs-lead)", { lineHeight: "var(--lh-lead)" }],
    body: ["var(--fs-body)", { lineHeight: "var(--lh-body)" }],
    small: ["var(--fs-small)", { lineHeight: "var(--lh-small)" }],
    xs2: ["var(--fs-xs2)", { lineHeight: "var(--lh-xs2)" }],
    label: [
      "var(--fs-label)",
      { lineHeight: "var(--lh-label)", letterSpacing: "var(--ls-label)" },
    ],
  },
  borderRadius: { card: "var(--radius-card)", pill: "var(--radius-pill)" },
  boxShadow: {
    sm: "var(--shadow-sm)",
    DEFAULT: "var(--shadow)",
    lg: "var(--shadow-lg)",
    lift: "var(--shadow-lift)",
    frame: "var(--shadow-frame)",
  },
  letterSpacing: { label: "var(--tracking-label)" },
  fontFamily: { sans: ["var(--font-sans)"], mono: ["var(--font-mono)"] },
  spacing: { section: "var(--sp-section)", gutter: "var(--sp-gutter)" },
  maxWidth: {
    page: "var(--w-page)",
    prose: "var(--w-prose)",
    measure: "var(--w-measure)",
  },
};
/* ==== END CANON ==== */

/* Canonical colours: the same key set in every palette on this surface.
   A key outside design-system/core.json > shared.colorKeys fails lint check 3. */
const COLORS = {
  brand: {
    "50": "var(--brand-50)",
    "400": "var(--brand-400)",
    "500": "var(--brand-500)",
  },
  ink: {
    "2": "var(--ink-2)",
    "3": "var(--ink-3)",
    DEFAULT: "var(--ink)",
    inverse: "var(--ink-inverse)",
  },
  line: { DEFAULT: "var(--line)", strong: "var(--line-strong)" },
  muted: "var(--muted)",
  page: "var(--page)",
  sunken: "var(--sunken)",
  surface: "var(--surface)",
};

/* Reserved: colours that carry meaning in ritesh-firodiya.github.io and may not be
   reused for anything else. Rule 01 — the one six of nine sets arrived at
   independently. See design-system/palettes/ritesh-firodiya.json for what each
   one means and why it is spoken for. */
const RESERVED = {
  beta: { DEFAULT: "var(--beta)", wash: "var(--beta-wash)" },
  build: { DEFAULT: "var(--build)", wash: "var(--build-wash)" },
  design: { DEFAULT: "var(--design)", wash: "var(--design-wash)" },
  live: { DEFAULT: "var(--live)", wash: "var(--live-wash)" },
  m: {
    ads: "var(--m-ads)",
    "ads-wash": "var(--m-ads-wash)",
    free: "var(--m-free)",
    "free-wash": "var(--m-free-wash)",
    once: "var(--m-once)",
    "once-wash": "var(--m-once-wash)",
    sub: "var(--m-sub)",
    "sub-wash": "var(--m-sub-wash)",
    year: "var(--m-year)",
    "year-wash": "var(--m-year-wash)",
  },
};

/* Affordances specific to ritesh-firodiya.github.io: a card lift, a pressed edge, a
   floating action button. Shared geometry stays in SCALES. */
const SHADOWS = {  };

/* Gradients are background images, so bg-<name> resolves as an image utility. */
const GRADIENTS = {  };

/* Deep merge, one level. A shallow spread let RESERVED's brand group replace
   COLORS' brand group wholesale, so declaring a reserved brand-x-from
   silently deleted brand-500 and thirteen uses of bg-brand-500 painted
   nothing. Groups are merged key by key. */
const merge = (base, extra) => {
  const out = { ...base };
  for (const [group, value] of Object.entries(extra)) {
    out[group] =
      value && typeof value === "object" && out[group] && typeof out[group] === "object"
        ? { ...out[group], ...value }
        : value;
  }
  return out;
};

tailwind.config = {
  theme: {
    extend: {
      ...SCALES,
      boxShadow: { ...SCALES.boxShadow, ...SHADOWS },
      backgroundImage: GRADIENTS,
      colors: merge(COLORS, RESERVED),
    },
  },
};
